SERIAL_PORT = "/dev/ttyUSB1" # probably shouldn't hard code this
PRIVATE_CHANNEL_NAME = "Home"
NODE_NAME = "mesh-bot"

STATE_FILE = "bot_state.json" #will be used for saving the state eventually. 

ADMIN_NODES = {
    #tbd
    #will be used for admin commands eventually. EG: reboot or something like that. 
}